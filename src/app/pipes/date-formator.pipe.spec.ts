import { DateFormatorPipe } from './date-formator.pipe';

describe('DateFormatorPipe', () => {
  it('create an instance', () => {
    const pipe = new DateFormatorPipe();
    expect(pipe).toBeTruthy();
  });
});
