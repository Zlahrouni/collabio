import { PriorityIconDirective } from './priority-icon.directive';

describe('PriorityIconDirective', () => {
  it('should create an instance', () => {
    const directive = new PriorityIconDirective();
    expect(directive).toBeTruthy();
  });
});
